# Written by tauri-build at each build: the schemas of the capabilities and the ACL manifests,
# and the allow- and deny- permissions of each command that build.rs declares.
/gen/
/permissions/autogenerated/
